#include <stdio.h>

/**
 * main - print all possible combinations of single digit numbers
 * Return: always 0
 */

int main(void)
{
	int i, j;

	i = '0';
	j = '0';

	for (i <= '9';)
	{
		putchar(i);
		putchar(',');
		putchar(' ');
		i++;
		for (j <= 9;)
		{
			putchar(ij);
			putchar(',');
			putchar(' ');
			ij++

		}
		putchar('\n');
	}
}
